pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Build Complete."'
                sh 'echo "Triggering from github."'
            }
        }
    }
}